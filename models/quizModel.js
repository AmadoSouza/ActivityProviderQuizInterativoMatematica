// /models/quizModel.js

// Simulação de "banco de dados" em memória
const quizzes = {}; // Armazena configurações de quiz (perguntas do aluno, etc.)
const respostas = {}; // Armazena respostas dos alunos

module.exports = {
  quizzes,
  respostas,
};
